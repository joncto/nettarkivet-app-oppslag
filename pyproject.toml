[project]
name = "nettarkivet-app-oppslag"
version = "0.2.0"
description = "Streamlit app for looking up URLs against Nettarkivet's CDX index"
requires-python = ">=3.14"
dependencies = [
    "streamlit==1.61.1",
    "pandas==3.0.5",
    "requests==2.34.2",
    "XlsxWriter==3.2.9",
]
